// Generated by CoffeeScript 1.7.1
(function() {
  var crt, defaultChannel, deleteLastLetter, enqueueRecentVideosForTag, enqueueVideo, enqueueVideosForTag, getVideoByTagAndId, introduction, playNextVideo, processNewMedia, setDefaultChannel, subscribeToNewChannel;

  enqueueVideo = function(v) {
    var video_data;
    video_data = {
      id: v._id,
      instagram_id: v.instagramId,
      preview: v.preview,
      sources: {
        hi: v.sources.hi,
        lo: v.sources.lo
      },
      details: {
        username: v.user.username,
        profile_picture: v.user.profile_picture,
        caption: v.caption,
        location: v.location
      }
    };
    return crt.enqueue(video_data);
  };

  processNewMedia = function(data) {
    console.log("New videos received");
    return $(data).each(function(index, media) {
      return enqueueVideo(media);
    });
  };

  introduction = function() {
    var $input;
    $input = $("input")[0];
    return deleteLastLetter($input);
  };

  deleteLastLetter = function($input) {
    var delay;
    window.tag = $input.value;
    delay = 1000 * Math.random();
    $input.focus();
    if (window.tag.length > 0) {
      return setTimeout((function() {
        $input.value = window.tag.slice(0, window.tag.length - 1);
        return deleteLastLetter($input);
      }), delay + 200);
    } else {
      return setDefaultChannel($input);
    }
  };

  setDefaultChannel = function($input) {
    var delay;
    window.tag = $input.value;
    delay = 800 * Math.random();
    $input.focus();
    if (window.tag.length !== defaultChannel.length) {
      return setTimeout((function() {
        $input.value += defaultChannel[window.tag.length];
        return setDefaultChannel($input);
      }), delay + 100);
    } else {
      return $("input").trigger("enterKey");
    }
  };

  playNextVideo = function() {
    return crt.playNext();
  };

  getVideoByTagAndId = function(tagName, id) {
    return $.get("/tag/" + tagName + "/video/" + id).done(function(data) {
      return data["videos"].forEach(function(v) {
        return enqueueVideo(v);
      });
    });
  };

  enqueueRecentVideosForTag = function(tagName) {
    return enqueueVideosForTag(tagName, null);
  };

  enqueueVideosForTag = function(tagName, minId) {
    var url;
    url = '/tag/' + tagName;
    if (minId) {
      url += '?minId=' + minId;
    }
    return $.ajax({
      type: 'GET',
      url: url,
      success: function(data) {
        if (data['videos'].length === 0) {
          setTimeout(function() {
            return enqueueVideosForTag(tagName, minId);
          }, 5000);
        }
        return data['videos'].forEach(function(v) {
          return enqueueVideo(v);
        });
      },
      error: function(XMLHttpRequest, textStatus, errorThrown) {
        if (XMLHttpRequest.status === 401) {
          $("body").toggleClass("overlay-active");
          return console.log('Attempted to create a subscription while unauthed.');
        }
      }
    });
  };

  crt = new window.CRT();

  defaultChannel = window.tag;

  subscribeToNewChannel = function(tagName) {
    crt.clearQueue();
    window.tag = tagName;
    console.log("tag submitted: " + tagName);
    return enqueueRecentVideosForTag(window.tag);
  };

  $().ready(function() {
    $("input").attr("value", window.tag);
    $("a#next").click(function() {
      playNextVideo();
      return enqueueVideosForTag(window.tag, crt.getMinId());
    });
    if (video_id) {
      getVideoByTagAndId(window.tag, video_id);
    }
    if (window.tag) {
      enqueueRecentVideosForTag(window.tag);
    }
    $("input").bind("enterKey", function(e) {
      var newTag;
      newTag = $(this)[0].value;
      return subscribeToNewChannel(newTag);
    });
    $("input").keyup(function(e) {
      var container, width;
      if (e.keyCode === 13) {
        $(this).trigger("enterKey");
      }
      $("#hidden").html($(this).val());
      width = $("#hidden").width() + 50;
      if (width < 600) {
        container = $(this).parent();
        return $(container).css({
          width: width
        });
      }
    });
    $("#submit").click(function() {
      return $("input").trigger("enterKey");
    });
    $("#overlay, #exit-modal").click(function() {
      return $("body").toggleClass("overlay-active");
    });
    $("#video_box").append(crt.$el_video);
    return $("#info-box").prepend(crt.$el_details);
  });

}).call(this);
